# Netlify Configuration for FNT Motor Group

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Build environment variables
[build.environment]
  NODE_VERSION = "20"

# Redirects for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
  conditions = {Role = []}

# Function settings (node bundler applies to all functions)
[functions]
  node_bundler = "esbuild"

# Scheduled Function: AutoTrader Stock Sync
# Runs once per day at 02:00 UTC as a fallback safety net.
# Webhooks handle all real-time updates; this is just a daily reconciliation.
# AutoTrader confirmed: maximum 3 API polls per day — we use 1.
[functions."sync-stock"]
  schedule = "0 2 * * *"  # Once per day at 02:00 UTC

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Headers for Netlify Functions
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Headers = "Content-Type, Authorization, X-Autotrader-Signature"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"

# Environment-specific settings
[context.production]
  [context.production.environment]
    AUTOTRADER_ENVIRONMENT = "production"

[context.deploy-preview]
  [context.deploy-preview.environment]
    AUTOTRADER_ENVIRONMENT = "sandbox"

[context.branch-deploy]
  [context.branch-deploy.environment]
    AUTOTRADER_ENVIRONMENT = "sandbox"
